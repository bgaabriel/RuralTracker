{
  "name": "ruraltracker",
  "version": "3.0.0",
  "private": true,
  "description": "Rural Tracker — gestão e monitoramento agrícola para produtores de café",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node --test tests/*.test.js",
    "build:catalogo": "node scripts/build-catalog.js"
  }
}
